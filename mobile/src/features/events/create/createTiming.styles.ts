import { StyleSheet } from 'react-native';

/**
 * Timing-section-specific styles.
 *
 * Currently all styles used by CreateTimingSection are shared form
 * primitives (formSection, sectionLabel, pillRow, stepper*, inlineError)
 * and live in create.styles.ts. Add timing-only overrides here as needed.
 */
export const createTimingStyles = StyleSheet.create({});
